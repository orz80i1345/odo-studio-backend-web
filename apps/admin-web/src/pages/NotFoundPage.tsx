/**
 * NotFoundPage.tsx — 404 找不到頁面
 * 骨架 placeholder：路徑不存在時顯示
 */
export function NotFoundPage() {
  return (
    <section>
      <h1 className="text-2xl font-bold text-ink">404 找不到頁面</h1>
      <p className="mt-2 text-ink-3">路徑不存在時顯示</p>
    </section>
  )
}
